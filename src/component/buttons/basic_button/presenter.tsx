import * as React from "react";

const Button: React.FC = text => {
  return (
    <button style={{ fontSize: "20px", backgroundColor: "red", border: "0" }}>
      text
    </button>
  );
};
export default Button;
